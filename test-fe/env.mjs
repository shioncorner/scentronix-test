import { createEnv } from '@t3-oss/env-nextjs';

/**
 * The ENV constant is used to define the environment variables that are used in the application.
 */
export const ENV = createEnv({
  server: {},
  client: {},
});
