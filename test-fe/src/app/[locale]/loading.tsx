import { Splash } from '~components/shared/splash';

/**
 * A component that displays a loading screen
 */
const Loading = () => <Splash className='min-h-with-out-header' />;

export default Loading;
